# Server
NODE_ENV=development
# Port configuration: 3000 for local development, 8080 for Docker/Fly production
SERVER_PORT=3000
SERVER_HOST=0.0.0.0

# CORS (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173,https://aipacto.com,https://www.aipacto.com,https://api.aipacto.com

# Auth
BETTER_AUTH_SECRET=change-me-too # Generate with: `openssl rand -base64 32`
# BETTER_AUTH_BASE_URL=https://api.aipacto.com
# BETTER_AUTH_COOKIE_DOMAIN=.aipacto.com

# Databases
DATABASE_AUTH_URL=file:./auth.db
DATABASE_AUTH_SECRET=change-me # Generate with: `openssl rand -base64 32`
DATABASE_URL=file:./server.db
# DATABASE_URL=libsql://<db-name>-<org-name>.turso.io
# DATABASE_TOKEN=your_turso_db_token
